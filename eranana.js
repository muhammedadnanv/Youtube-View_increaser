if (navigator.onLine) {
    // The user is online, so assume they are connected to some network
    window.alert('Welcome to Our YouTube Multi-Viewer  | thank you For Using Our Service');
    
} else {
    // The user is offline, handle accordingly
    console.log('The user is offline');
    // You can provide an alternative action here if needed
}
